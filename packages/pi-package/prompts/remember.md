Store the following as a durable operational fact in memory.

Fact:        {{fact}}
Environment: {{env}}
Confidence:  {{confidence}}

Entity names this fact is about (comma-separated, or leave blank):
{{entities}}

Evidence (optional — a URL, commit SHA, or alert ID that supports this):
{{evidence}}

Use memory_store to save this. Confirm what was stored.
